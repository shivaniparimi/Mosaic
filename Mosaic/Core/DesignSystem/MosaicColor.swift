import SwiftUI

enum MosaicColor {
    static let accent = Color(hex: "#E8738A")
    static let canvas = Color(light: "#F7F5F1", dark: "#1C1C1E")
    static let surface = Color(light: "#FFFFFF", dark: "#2C2C2E")
    static let phoneSurround = Color(hex: "#E2DDD6")
    static let destructive = Color(hex: "#FF3B30")
    static let tagPurple = Color(hex: "#AF52DE")
}
